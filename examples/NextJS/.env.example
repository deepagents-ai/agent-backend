# Core Configuration
AGENTBE_WORKSPACE_ROOT=/tmp/agentbe-workspace

# AI Configuration (Required)
OPENROUTER_API_KEY=sk-or-v1-...

# Remote Backend Configuration (Optional - only if NEXT_PUBLIC_BACKEND_TYPE=remote)
REMOTE_VM_HOST=example.com
REMOTE_VM_USER=agent
REMOTE_VM_PASSWORD=secret
# Or use private key instead of password:
# REMOTE_VM_PRIVATE_KEY=/path/to/key

# Remote MCP Server URL (Required for remote backend)
REMOTE_MCP_URL=http://example.com:3001

# Optional: MCP server authentication token
REMOTE_MCP_AUTH_TOKEN=token123
