# AI Configuration (Required)
OPENROUTER_API_KEY=sk-or-v1-...

# Note: All backend configuration (type, workspace root, remote connection)
# is now managed via the UI. Click the settings icon in the header.
