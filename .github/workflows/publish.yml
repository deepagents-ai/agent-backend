name: Publish Packages

on:
  workflow_dispatch:

jobs:
  check-version:
    runs-on: ubuntu-latest
    outputs:
      should_publish_npm: ${{ steps.check.outputs.should_publish_npm }}
      should_publish_pypi: ${{ steps.check.outputs.should_publish_pypi }}
      should_publish_opensdd: ${{ steps.check.outputs.should_publish_opensdd }}
      version: ${{ steps.check.outputs.version }}
    steps:
      - uses: actions/checkout@v7

      - name: Check if versions are already published
        id: check
        run: |
          VERSION=$(jq -r '.version' packages/agent-backend/typescript/package.json)
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

          if npm view "agent-backend@$VERSION" version 2>/dev/null; then
            echo "v$VERSION already published to npm — skipping"
            echo "should_publish_npm=false" >> "$GITHUB_OUTPUT"
          else
            echo "v$VERSION not yet on npm — will publish"
            echo "should_publish_npm=true" >> "$GITHUB_OUTPUT"
          fi

          if curl -sf "https://pypi.org/pypi/agent-backend/$VERSION/json" >/dev/null 2>&1; then
            echo "v$VERSION already published to PyPI — skipping"
            echo "should_publish_pypi=false" >> "$GITHUB_OUTPUT"
          else
            echo "v$VERSION not yet on PyPI — will publish"
            echo "should_publish_pypi=true" >> "$GITHUB_OUTPUT"
          fi

          # `opensdd publish` opens a PR from agent-backend/<version>; treat an existing
          # branch (PR pending) or registry entry (PR merged) as already published.
          if git ls-remote --exit-code --heads https://github.com/deepagents-ai/opensdd "agent-backend/$VERSION" >/dev/null 2>&1 \
            || curl -sf "https://raw.githubusercontent.com/deepagents-ai/opensdd/main/registry/agent-backend/$VERSION/manifest.json" >/dev/null 2>&1; then
            echo "v$VERSION already published to OpenSDD registry — skipping"
            echo "should_publish_opensdd=false" >> "$GITHUB_OUTPUT"
          else
            echo "v$VERSION not yet in OpenSDD registry — will publish"
            echo "should_publish_opensdd=true" >> "$GITHUB_OUTPUT"
          fi

  publish-typescript:
    needs: check-version
    if: needs.check-version.outputs.should_publish_npm == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write  # Required for OIDC
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: 22
          registry-url: "https://registry.npmjs.org"

      - uses: pnpm/action-setup@v6
        with:
          version: 10

      - name: Upgrade npm for OIDC trusted publishing
        run: npm install -g npm@11.11.1

      - name: Install dependencies
        working-directory: packages/agent-backend/typescript
        run: pnpm install --frozen-lockfile

      - name: Build
        working-directory: packages/agent-backend/typescript
        run: pnpm run build

      - name: Publish to npm
        working-directory: packages/agent-backend/typescript
        run: npm publish --provenance --access public

  publish-python:
    needs: check-version
    if: needs.check-version.outputs.should_publish_pypi == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write  # Required for PyPI OIDC trusted publishing
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v7

      - name: Install dependencies
        run: uv sync

      - name: Build
        run: uv build --package agent-backend

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: dist/

  publish-opensdd:
    needs: check-version
    if: needs.check-version.outputs.should_publish_opensdd == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: 22

      - name: Install opensdd CLI
        run: npm install -g opensdd

      - name: Configure git identity and credentials
        env:
          GH_TOKEN: ${{ secrets.GH_ACCESS_TOKEN }}
        run: |
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git config --global user.name "github-actions[bot]"
          gh auth setup-git

      - name: Publish OpenSDD spec
        working-directory: packages/agent-backend
        env:
          GH_TOKEN: ${{ secrets.GH_ACCESS_TOKEN }}
        run: opensdd publish --branch "agent-backend/${{ needs.check-version.outputs.version }}"

  tag-release:
    needs: [check-version, publish-typescript, publish-python, publish-opensdd]
    # Skipped publish jobs mean that version was already published, so only a failed or
    # cancelled job blocks the tag. Re-running after a partial failure completes the release.
    if: always() && !contains(needs.*.result, 'failure') && !contains(needs.*.result, 'cancelled')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v7

      - name: Create and push tag
        run: |
          VERSION="v${{ needs.check-version.outputs.version }}"
          if git ls-remote --tags origin "$VERSION" | grep -q "$VERSION"; then
            echo "Tag $VERSION already exists on remote — skipping"
          else
            git tag "$VERSION"
            git push origin "$VERSION"
          fi
