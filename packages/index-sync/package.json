{
  "name": "@agentbe/index-sync",
  "version": "0.0.0",
  "private": true,
  "description": "Keep a semantic + lexical index in step with a versioned-store room; BYO embedding provider and vector store",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "src"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest",
    "test:run": "vitest run",
    "test:unit": "vitest run"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@agentbe/versioned-store": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.17.10",
    "typescript": "^7.0.2",
    "vitest": "^3.2.6"
  }
}
